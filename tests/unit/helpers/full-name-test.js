import { fullName } from '../../../helpers/full-name';
import { module, test } from 'qunit';

module('Unit | Helper | full name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fullName(42);
  assert.ok(result);
});
